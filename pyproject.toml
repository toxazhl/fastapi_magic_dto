[project]
name = "fastapi-magic-dto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "uvicorn>=0.41.0",
]
